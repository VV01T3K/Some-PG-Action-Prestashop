[project]
name = "selenium-grid"
version = "0.1.0"
description = "Selenium Grid with Chrome and Firefox for automated browser testing"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=8.4.2",
    "selenium>=4.37.0",
]
